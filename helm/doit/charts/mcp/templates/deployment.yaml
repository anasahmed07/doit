apiVersion: apps/v1
kind: Deployment
metadata:
  name: doit-mcp
  labels:
    app: doit-mcp
    app.kubernetes.io/part-of: doit
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: doit-mcp
  template:
    metadata:
      labels:
        app: doit-mcp
        app.kubernetes.io/part-of: doit
    spec:
      containers:
        - name: mcp
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: doit-mcp-config
            - secretRef:
                name: doit-mcp-secret
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
