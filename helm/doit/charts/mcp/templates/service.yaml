apiVersion: v1
kind: Service
metadata:
  name: doit-mcp
  labels:
    app: doit-mcp
    app.kubernetes.io/part-of: doit
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
  selector:
    app: doit-mcp
