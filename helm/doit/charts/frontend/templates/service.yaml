apiVersion: v1
kind: Service
metadata:
  name: doit-frontend
  labels:
    app: doit-frontend
    app.kubernetes.io/part-of: doit
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
  selector:
    app: doit-frontend
